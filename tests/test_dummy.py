"""Dummy test to verify TCR script works."""


def test_always_passes() -> None:
    """A test that always passes."""
    assert True


def test_also_passes() -> None:
    """Another test that passes."""
    assert 1 + 1 == 2
